#include "main.h"

/**
 * _isupper - checks for uppercase letter.
 * @x: the letter to check for
 * Return: 1 if uppercase, 0 if otherwise.
 */
int _isupper(int x);
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}

#include "main.h"

/**
 * print_triangle - prints a triangle using the character '#'
 * @size: size of triangle
 */
void print_triangle(int size)
{
	int x, y;

	if (size > 0)
	{
		for (y = size - x; y > 0; index--)
		{
			_putchar(' ');
		}
		for (y = 0; y < x; y++)
		{
			_putchar('#');
		}
		if (x == size)
		{
			continue;
		}
		_putchar('\n');
	}
	_putchar('\n');
}
